# 급식 젯슨 코드 수정 방법 (간단 버전)

**기존 코드에 추론 로직이 이미 있습니다!** 최소한의 수정만 하면 됩니다.

기존 `stream_server_ros.py`의 `infer_loop` 함수를 재사용하는 방법입니다.

---

## ✅ 기존 코드 활용

**이미 구현된 것:**
- ✅ YOLO 세그멘테이션 (`yolo.predict()`)
- ✅ ROI 추출 (`get_roi_from_seg_result()`)
- ✅ Regression 모델 추론 (`predict_grams()`)
- ✅ 퍼센트 계산 (`grams / full_grams * 100`)

**추가해야 할 것:**
- `feed/request` 토픽 구독
- `feed/amount` 토픽 발행
- 요청 시 추론 실행

---

## 🔧 최소 수정 방법 (기존 코드 재사용)

### 방법 1: 기존 `infer_loop` 결과 활용 (가장 간단)

기존 `infer_loop` 함수는 이미 추론을 하고 있습니다. 그 결과를 저장해서 `feed/request` 신호를 받으면 `feed/amount`로 발행:

```python
# stream_server_ros.py에 추가 (최소 수정)

from std_msgs.msg import String, UInt8

# 전역 변수로 최신 추론 결과 저장
LATEST_FEED_LEVEL = None
FEED_LEVEL_LOCK = threading.Lock()

# 기존 infer_loop 함수 수정 (한 줄만 추가)
def infer_loop(args, sub: ImageSub):
    global LATEST_JPEG, LATEST_FEED_LEVEL  # 추가
    
    # ... 기존 코드 그대로 ...
    
    while True:
        # ... 기존 추론 코드 그대로 ...
        
        if idx is None:
            # ... 기존 코드 ...
        else:
            roi, bbox = get_roi_from_seg_result(r, frame, idx, margin=args.margin)
            if roi is None or roi.size == 0:
                # ... 기존 코드 ...
            else:
                grams = predict_grams(reg_model, reg_tf, roi, device=args.device, amp=args.amp)
                pct = clamp((grams / full_grams) * 100.0, 0.0, 100.0)
                
                # 레벨 변환 및 저장 (추가)
                if pct < 33.0:
                    feed_level = 1
                elif pct < 66.0:
                    feed_level = 2
                else:
                    feed_level = 3
                
                with FEED_LEVEL_LOCK:
                    LATEST_FEED_LEVEL = feed_level
                
                # 기존 시각화 코드 그대로 ...
                cv2.putText(vis, f"{grams:.1f} g ({pct:.0f}%)", ...)

# FeedAINode 클래스 추가 (간단)
class FeedAINode(Node):
    def __init__(self):
        super().__init__("feed_ai_node")
        self.sub_feed_request = self.create_subscription(
            String, "feed/request", self.on_feed_request, 10
        )
        self.pub_feed_amount = self.create_publisher(UInt8, "feed/amount", 10)
    
    def on_feed_request(self, msg: String):
        """Pi Gateway가 급식 요청 시 최신 추론 결과 발행"""
        try:
            import json
            data = json.loads(msg.data)
            waypoint_id = data.get("waypoint_id", "")
            self.get_logger().info(f"Feed requested for waypoint: {waypoint_id}")
            
            # 기존 infer_loop가 계산한 최신 결과 사용
            with FEED_LEVEL_LOCK:
                feed_level = LATEST_FEED_LEVEL if LATEST_FEED_LEVEL is not None else 2
            
            msg = UInt8()
            msg.data = feed_level
            self.pub_feed_amount.publish(msg)
            self.get_logger().info(f"Published feed/amount: {feed_level}")
        except Exception as e:
            self.get_logger().error(f"Failed to process feed request: {e}")

# main() 함수 수정
def main():
    # ... 기존 코드 ...
    
    rclpy.init()
    sub = ImageSub(args.topic)
    feed_node = FeedAINode()  # 추가
    
    th_spin = threading.Thread(target=rclpy.spin, args=(feed_node,), daemon=True)  # 수정
    th_spin.start()
    
    th_infer = threading.Thread(target=infer_loop, args=(args, sub), daemon=True)
    th_infer.start()
    
    app.run(host=args.host, port=args.port, threaded=True)
```

---

### 방법 2: 기존 `infer_loop` 함수 수정

기존 `infer_loop` 함수에 `feed/request` 플래그 추가:

```python
# 기존 infer_loop 함수 수정

def infer_loop(args, sub: ImageSub, feed_node=None):
    """기존 함수에 feed_node 파라미터 추가"""
    global LATEST_JPEG
    
    full_grams = load_full_grams(Path(args.config))
    yolo = YOLO(str(args.yolo_seg))
    reg_model, reg_tf, backbone, reg_imgsz = load_regression(Path(args.reg_ckpt), args.device)
    
    # ... 기존 코드 ...
    
    while True:
        frame, ts = sub.get_latest()
        
        # feed/request 신호 확인
        if feed_node and feed_node.pending_request:
            # 급식 요청이 있으면 추론 실행
            r = yolo.predict(source=frame, imgsz=args.imgsz, conf=args.conf, verbose=False)[0]
            idx, reason = choose_best_instance_by_mask_area(r, conf_thres=args.conf, min_mask_area_ratio=args.min_mask_area_ratio)
            
            if idx is None:
                feed_level = 2
            else:
                roi, bbox = get_roi_from_seg_result(r, frame, idx, margin=args.margin)
                if roi is None or roi.size == 0:
                    feed_level = 2
                else:
                    grams = predict_grams(reg_model, reg_tf, roi, device=args.device, amp=args.amp)
                    percent = clamp((grams / full_grams) * 100.0, 0.0, 100.0)
                    
                    if percent < 33.0:
                        feed_level = 1
                    elif percent < 66.0:
                        feed_level = 2
                    else:
                        feed_level = 3
                
                # feed/amount 발행
                msg = UInt8()
                msg.data = feed_level
                feed_node.pub_feed_amount.publish(msg)
                feed_node.pending_request = None
        
        # 기존 추론 로직은 그대로...
        # ...
```

---

## 💡 가장 간단한 방법

**기존 코드를 최대한 활용:**

1. `stream_server_ros.py`에 토픽만 추가
2. `feed/request` 받으면 기존 추론 함수 호출
3. 결과를 `feed/amount`로 발행

**추가할 코드 (최소):**

```python
# stream_server_ros.py 상단에 추가
from std_msgs.msg import String, UInt8

# FeedAINode 클래스 추가 (기존 코드 재사용)
class FeedAINode(Node):
    def __init__(self):
        super().__init__("feed_ai_node")
        self.sub_feed_request = self.create_subscription(String, "feed/request", self.on_feed_request, 10)
        self.pub_feed_amount = self.create_publisher(UInt8, "feed/amount", 10)
        # 기존 모델 로드 코드 재사용
        # 기존 추론 함수 재사용
    
    def on_feed_request(self, msg: String):
        # 기존 infer_loop의 추론 로직 호출
        # 결과를 feed/amount로 발행
```

---

## 📝 요약

**기존 코드 활용:**
- ✅ YOLO 세그멘테이션: `yolo.predict()` 재사용
- ✅ ROI 추출: `get_roi_from_seg_result()` 재사용
- ✅ Regression 추론: `predict_grams()` 재사용
- ✅ 퍼센트 계산: 기존 로직 재사용

**추가만 하면 됨:**
- `feed/request` 구독
- `feed/amount` 발행
- 요청 시 기존 추론 함수 호출

**결론:** 기존 코드를 최대한 재사용하고, 토픽만 추가하면 됩니다!
